subdir('lib')

# NOTE  This must occur after the lib, since mrc_reuse_stack depends on lib
subdir('mrc')

subdir('ttl')

# NOTE  This depends on 'lib' and 'mrc'.
subdir('analysis')

# NOTE  This depends on 'lib' and 'mrc'.
subdir('run')